============
Installation
============

At the command line::

    $ easy_install loganalysis

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv loganalysis
    $ pip install loganalysis
